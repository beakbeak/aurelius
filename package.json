{
  "type": "module",
  "dependencies": {
    "@sveltejs/vite-plugin-svelte": "^7.0.0",
    "@types/jsdom": "^21.1.7",
    "@types/mocha": "^10.0.10",
    "@types/node": "^22.13.10",
    "@types/wicg-mediasession": "^1.1.8",
    "svelte": "^5.55.0",
    "typescript": "^5.8.2",
    "@tailwindcss/vite": "^4.2.2",
    "daisyui": "^5.5.19",
    "tailwindcss": "^4.2.2",
    "vite": "^8.0.7"
  },
  "devDependencies": {
    "@playwright/cli": "^0.1.1",
    "jsdom": "^26.0.0",
    "mocha": "^11.7.5",
    "mocha-suppress-logs": "^0.6.0",
    "nyc": "^17.1.0",
    "svelte-check": "^4.4.5",
    "tsx": "^4.21.0"
  },
  "scripts": {
    "gobuild": "go build -tags sqlite_fts5 ./cmd/aurelius",
    "gotest": "LSAN_OPTIONS=suppressions=${PWD}/test/lsan.supp go test -tags sqlite_fts5 -asan ./...",
    "gzip-assets": "find cmd/aurelius/assets -type f \\( -name '*.js' -o -name '*.css' -o -name '*.html' \\) -exec gzip -k -f -9 {} +",
    "build": "vite build && npm run gzip-assets",
    "build-debug": "svelte-check && vite build --mode development --minify false && npm run gzip-assets",
    "mocha": "mocha --require mocha-suppress-logs",
    "test": "killall aurelius; go run ./cmd/aurelius -log error -media test/media -storage test/storage & npm run mocha; killall aurelius",
    "coverage": "nyc --reporter=lcov --reporter=text mocha"
  },
  "mocha": {
    "extension": [
      "ts"
    ],
    "spec": "ts/**/*.spec.ts",
    "require": [
      "tsx/cjs"
    ]
  }
}
